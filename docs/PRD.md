# 그리디 허브 (Greedy Hub) — PRD

> **상태**: v0.1 초안 (티키타카 진행 중) · **작성 목적**: 메인테이너 설득 + 기획 확정
> **한 줄 요약**: 그리디의 *연혁·멤버·스터디·미션·게시판·지원·추억* 을 한곳에서 추적하는 동아리 공식 허브.

---

## 1. 왜 지금 만드는가 (배경 & 문제 정의)

그리디는 활발히 굴러가지만, **활동의 기록이 한곳에 남지 않는다.** 정보가 Discord 스크롤, 개인 노션, 구글폼, 단톡, 개인 폰 사진첩으로 흩어져 있고, 기수가 바뀌면 그 맥락이 사라진다.

설득 포인트 — 지금 우리가 겪는 구체적 통증:

| # | 페인포인트 | 지금 | 결과 |
|---|---|---|---|
| P1 | **미션 현황 추적 불가** | 제출이 Discord 요청·PR로 흩어짐 | 누가 어디까지 했는지 한눈에 안 보임, 완주율 모름 |
| P2 | **기록 휘발** | 연혁·추억이 단톡/개인저장소에 | 기수 넘어가면 그리디의 역사가 소실 |
| P3 | **신원·권한 부재** | 누가 몇 기·운영진인지 흩어짐 | 권한별 접근/관리 불가 |
| P4 | **자료 분산** | 스터디 자료가 노션/드라이브/단톡 | "그거 어디 있더라"의 반복 |
| P5 | **지원 관리 수기** | 구글폼 + 수기 | 지원자 상태 추적이 흩어짐 |
| P6 | **대외 얼굴 없음** | 공식 홈페이지 없음 | 지원자·외부에 보여줄 단일 창구 부재 |

> **핵심 통찰**: 우리는 도구를 *추가*하려는 게 아니라, 흩어진 것을 **한 층(웹)으로 모으고**, 이미 쓰는 도구(GitHub·Discord)는 각자 잘하는 역할만 맡긴다.
>
> **웹 = 기록(진실의 원천) / GitHub = 산출물 / Discord = 알림**

---

## 2. 목표 / 비목표

**목표 (이번 범위가 이루려는 것)**
- G1. 미션 출제→제출(GitHub PR)→리뷰→**현황 추적**을 이중작업 없이 한곳에서.
- G2. 연혁·멤버·추억을 **영구 기록**으로 — 기수가 바뀌어도 남는다.
- G3. GitHub 로그인 기반 **신원·기수·권한** 체계.
- G4. 스터디 자료·커리큘럼을 검색 가능한 단일 저장소로.
- G5. 지원 접수→심사→멤버 전환을 한 플로우로.
- G6. 지원자·외부에 보여줄 **공개 페이지**(SEO).

**비목표 (이번엔 안 한다 — 범위 방어)**
- N1. Discord를 대체하지 않는다. (실시간 대화는 계속 Discord)
- N2. 자체 코드 채점/CI 빌드를 처음부터 만들지 않는다. (미션 자동검증은 후속 단계)
- N3. 모바일 네이티브 앱 없음. (반응형 웹으로 충분)
- N4. 결제·회비 정산 시스템 없음. (필요 시 별도 논의)

---

## 3. 성공 지표

- **북극성 지표**: *기수 미션 완주율을 웹 대시보드에서 한눈에 볼 수 있다* (현재 0 → 가능).
- 보조 지표
  - 미션 제출의 N%가 웹에 기록됨 (목표: MVP 후 1개 기수 100%).
  - 회원의 활성 로그인 비율.
  - 연혁/추억 아카이브에 쌓인 항목 수(휘발 방지).
  - 지원 시즌의 지원서가 웹으로 100% 수집됨.

---

## 4. 사용자 & 역할

**페르소나**
- **지원자/방문자(Guest)**: 그리디가 뭐 하는 곳인지, 어떻게 지원하는지 보고 싶다.
- **회원(Member)**: 내 미션 현황·자료·게시판을 보고, 결과물을 제출/공유한다.
- **리뷰어(Reviewer)**: 배정된 미션 PR에서 멤버와 리뷰 티키타카를 진행한다. (메인테이너 겸임 또는 리뷰 전담)
- **운영진/메인테이너·리드(Staff)**: 미션을 출제하고, 멤버·지원·콘텐츠·권한을 관리한다.

**역할은 두 축으로 본다.**

1) **글로벌 역할(서열 고정, 가입 시 부여)**
```
GUEST  →  MEMBER  →  STAFF
```

2) **기수 역할 (CohortMember.roleInCohort — 기수별로 달라짐, 발자취에 표기)**
```
MEMBER   리드(LEAD)   리뷰어(REVIEWER)   메인테이너(MAINTAINER)
```
- 기수 = **입회 기수**(한 학기당 1기수, 연 2기수 / 현재 **4기** 진행 중). 한 사람이 여러 기수에 다른 역할로 등장할 수 있다(예: 2기에 멤버로 들어와 4기에 리뷰어).
- 기수 역할이 권한을 **스코프**해서 부여한다: 리뷰어 → 배정된 미션에 `mission:review`, 리드/메인테이너 → STAFF급 관리권.

세분화된 권한은 **운영진이 UI에서 개별 부여**할 수 있다(아래 RBAC). "처음부터 과하게 안 짜되, 필요할 때 쪼갤 수 있게." — 이 권한 관리 UI는 **MVP에 포함**.

---

## 5. 권한 모델 (RBAC)

- **역할(Role)**: 가입 시 부여, 서열 고정 — `GUEST / MEMBER / STAFF`.
- **세부 권한(Permission)**: `mission:create`, `mission:review`, `mission:assign`(리뷰어 배정), `board:moderate`, `recruit:review`, `gallery:curate`, `history:edit`, `study:manage`, `member:manage`, `admin:roles` …
- **실효 권한 = 글로벌 역할 기본 권한 ∪ 기수 역할이 주는 스코프 권한 ∪ 운영진이 개별 부여한 권한.**
  - 예: 평소 MEMBER지만 4기 리뷰어로 배정되면 그 미션들에 한해 `mission:review` 획득.
  - 예: 한시적으로 출제를 맡기면 `mission:create`만 콕 부여.
- **소유권 규칙**: 역할과 별개로 "본인이 만든 글/제출만 수정" 항상 적용.
- **보안 원칙**: 권한 체크의 진짜 방어선은 **백엔드**. 프론트는 UI 숨김(편의)만 담당.

> 상세 권한 매트릭스는 [부록 B](#부록-b-권한-매트릭스).

---

## 6. 기능 요구사항

각 기능: *목적 / 페인포인트 / 핵심 기능 / 사용자 스토리 / 권한*.

### 6.1 인증 & 온보딩
- **목적**: GitHub 로그인 한 번으로 신원·기수·권한·깃허브 연동.
- **페인포인트**: P3. 신원/권한 개념 부재, 깃허브-동아리 신원 분리.
- **핵심 기능**
  - GitHub OAuth 로그인. 최초엔 `GUEST`.
  - 프로필에 GitHub username/아바타 자동 연동 (미션 PR 매칭 키).
  - `MEMBER` 승격: 지원 합격 시 자동, 또는 운영진 수동.
- **사용자 스토리**
  - 방문자로서 GitHub로 로그인해 회원 공간에 접근하고 싶다.
  - 운영진으로서 한 멤버의 역할/권한을 바꾸고 싶다.
- **권한**: 로그인 전체 / 역할·권한 변경 `admin:roles`.

### 6.2 미션 (Mission) ⭐ 최우선
- **목적**: 출제→GitHub PR 제출→리뷰어 티키타카→머지(완료)→추적을 **이중작업 없이**.
- **페인포인트**: P1. 제출·리뷰가 Discord/PR로 흩어져 기록 안 남고, 누가 리뷰 중·완료인지·완주율을 한눈에 못 봄.
- **현재 흐름(그리디 실제 방식 반영)**:
  1. 멤버가 미션 레포(예: `self-paced-react-advanced`)를 fork→작업→**PR**(`[adv-2.1단계] 이름 미션 제출합니다`).
  2. **매 미션마다 리뷰어가 랜덤 배정**된다. (리뷰어 = 전담 또는 메인테이너 겸임)
  3. 멤버↔리뷰어가 PR에서 **리뷰 티키타카**(여러 라운드)를 진행. **정해진 리뷰 기간**이 있고, **리뷰어 재량으로 연장 가능**.
  4. **리뷰 기간이 끝나면 자동 머지** → 리뷰 종료(=완료). *approve가 곧 통과가 아니라, 티키타카가 끝나는 개념.*
- **핵심 기능**
  - **미션 정의**: 제목·설명·**요구사항 체크리스트**·기본 리뷰 기간·대상 기수·연결 GitHub 레포·단계 라벨.
  - **제출 매칭**: 멤버는 평소대로 PR을 올리고, 웹은 GitHub username·PR 제목 규칙으로 **그 PR을 자동 매칭/표시**. 보조로 수동 링크 등록 가능.
  - **리뷰어 배정**: 랜덤 배정은 **기존 디스코드 봇 `/리뷰매칭`이 수행**(8.1). 웹은 봇이 보낸 결과를 **기록**하고(담당 리뷰어 표시), 필요시 수동 재배정. 봇은 명단을 웹의 기수 멤버에서 조회.
  - **리뷰 진행 추적**: 상태(제출됨/리뷰중/완료), **리뷰 마감일(연장 이력 포함)**, 라운드 수, PR 링크. *실제 대화는 GitHub PR에 두고 웹은 상태·기한만 추적(GitHub=산출물 원칙).*
  - **리뷰 기간**: **동아리 공통 기본값**(전역 설정) + 미션별 예외값(`defaultReviewDays` override) + 리뷰어 연장.
  - **트래킹 3종**: ① 멤버 "내 미션 현황"(리뷰중/완료/마감임박) ② 운영진 `멤버×미션` 매트릭스 ③ 기수 완주율 대시보드.
  - **알림**: 배정·새 리뷰 코멘트·머지(완료)를 Discord 웹훅으로.
- **GitHub 연동 깊이 (단계적)**
  - 1단계(MVP): PR 링크 등록 + 리뷰어 배정·상태·마감일 수동 관리 + Discord 알림. (흩어진 흐름을 "기록되는 버전"으로 이전)
  - 2단계: GitHub API로 PR 상태·리뷰 코멘트 수·머지 여부를 자동 동기화.
  - 3단계: 리뷰 기간 종료 시 **자동 머지 트리거**, 요구사항 일부 자동검증.
- **사용자 스토리**
  - 멤버로서 내가 리뷰 중인/마감 임박한 미션을 한눈에 보고 싶다.
  - 리뷰어로서 내게 배정된 PR과 리뷰 마감일을 모아 보고, 필요시 기간을 연장하고 싶다.
  - 운영진으로서 기수 완주율과 누가 어느 단계를 빠뜨렸는지 매트릭스로 보고 싶다.
- **권한**: 출제·요구사항 `mission:create` / 리뷰어 배정 `mission:assign` / 리뷰·기간연장 `mission:review`(배정된 리뷰어) / 제출·조회 `MEMBER`.
- **상세 상태머신**: [부록 C](#부록-c-미션-상태머신).

### 6.3 연혁 (History / Timeline)
- **목적**: 그리디가 걸어온 길 — 공개 페이지로 보여줄 얼굴.
- **페인포인트**: P2. 기록 휘발.
- **핵심 기능**: 연도/기수별 타임라인, 이벤트 카드(제목·날짜·사진·설명), 공개 열람.
- **사용자 스토리**: 방문자로서 그리디의 역사를, 운영진으로서 새 이벤트를 추가하고 싶다.
- **권한**: 열람 전체 / 작성·수정 `history:edit`.

### 6.4 멤버 (Members / 발자취)
- **목적**: 멤버별 프로필 + 활동 발자취(기수별 맡은 역할·완료 미션·작성 글).
- **페인포인트**: P2/P3. "이 사람 몇 기·무슨 역할·뭐 했지?" 추적 불가, 비활동 멤버 기록 소실.
- **핵심 기능**: 공개 프로필(이름·깃허브·입회기수·한줄소개), **기수별 역할 발자취**(예: "2기 멤버 → 4기 리뷰어"), 완료 미션 수, 기수별 디렉토리.
- **사용자 스토리**: 본인 프로필 수정 / 운영진의 기수 지정·멤버 관리.
- **권한**: 공개 프로필 열람 전체 / 본인 수정 / 관리 `member:manage`.

### 6.5 스터디 (Study)
- **목적**: 자료 + 커리큘럼(요구사항) 단일 저장소.
- **페인포인트**: P4. 자료 분산.
- **핵심 기능**: 주차/단계별 커리큘럼, 자료 업로드(링크·파일), 검색·태그.
- **사용자 스토리**: 멤버의 자료 업로드 / 운영진의 커리큘럼 편집.
- **권한**: 멤버 열람+업로드 / 커리큘럼 편집 `study:manage`.

### 6.6 게시판 (Board)
- **목적**: 공지·자유·질문 글과 댓글의 아카이브.
- **페인포인트**: 공지가 Discord 스크롤에 묻히고 검색·보존 불가.
- **핵심 기능**: 카테고리(공지/자유/질문), 글·댓글 CRUD, 공지 고정, 검색.
- **사용자 스토리**: 멤버의 글·댓글 / 운영진의 공지·모더레이션.
- **권한**: 공개글 열람 전체 / 작성·댓글 `MEMBER` / 모더레이션 `board:moderate` / 본인 글만 수정.

### 6.7 지원 (Recruit)
- **목적**: 비회원 지원→**단계 심사(서류→면접→합격)**→멤버 전환을 한 플로우로.
- **페인포인트**: P5. 구글폼+수기, 상태 추적 분산.
- **핵심 기능**
  - **웹 자체 지원 폼**(모집기간 노출) → 응답이 곧장 웹 `Application`으로 저장. (구글폼 의존 탈피. 기존 봇의 구글폼 감시는 설문 등 다른 용도로 잔존 가능.)
  - 본인 지원 현황 조회.
  - 운영진 **심사 보드**: 단계 칸반(서류 → 면접 → 합격/불합격), 단계 이동·코멘트.
  - 합격 시 `MEMBER` 승격 + 해당 기수 `CohortMember` 생성 연동.
- **단계 상태**: `SUBMITTED → DOC_PASS → INTERVIEW → ACCEPTED | REJECTED`.
- **사용자 스토리**: 방문자의 지원 / 운영진의 단계별 심사·합격 처리.
- **권한**: 제출 `GUEST` / 본인 조회 / 심사 `recruit:review`.

### 6.8 추억 저장소 (Gallery)
- **목적**: 사진·영상·회고 아카이브.
- **페인포인트**: P2. 행사 사진이 개인 폰/단톡에 흩어져 소실.
- **핵심 기능**: 앨범(이벤트·기수별), 미디어 업로드, 회고 글, 공개/회원전용 구분.
- **사용자 스토리**: 멤버의 업로드 / 운영진의 큐레이션.
- **권한**: 공개 갤러리 열람 전체 / 업로드 `MEMBER` / 큐레이션·삭제 `gallery:curate`.

---

## 7. 정보 구조 (사이트맵)

```
공개 (비로그인 열람)
├─ / (홈 — 그리디 소개·하이라이트)
├─ /about (연혁/타임라인)
├─ /members (멤버 디렉토리·발자취)
├─ /gallery (공개 추억)
├─ /recruit (모집 안내·지원 폼)
└─ /board (공개 공지)

회원 전용 (로그인)
├─ /missions (내 미션 현황 · 미션 목록)
│   └─ /missions/[id] (요구사항·제출·리뷰)
├─ /study (커리큘럼·자료)
├─ /board (작성·댓글)
└─ /me (내 프로필)

운영진 전용
└─ /admin
    ├─ /admin/missions (출제·리뷰·매트릭스 대시보드)
    ├─ /admin/members (역할·권한·기수)
    ├─ /admin/recruit (심사 보드)
    ├─ /admin/history (연혁 편집)
    └─ /admin/gallery (큐레이션)
```

---

## 8. 시스템 아키텍처

- **프론트엔드**: Next.js (App Router) — 공개 페이지 SSR/SEO, 회원/운영진은 인증 기반 라우팅. TypeScript + Tailwind.
- **백엔드**: 운영진이 별도 언어로 구현(Java/Python/Go 등).
- **계약(Contract)**: **REST + OpenAPI 단일 스펙(`openapi.yaml`)**.
  - 백엔드가 스펙을 단일 소스로 관리 → 프론트는 `openapi-typescript`로 **타입·클라이언트 자동생성**.
  - 프론트는 스펙 기반 **목 서버**로 백엔드 없이 선행 → 병렬 개발.
- **인증**: GitHub OAuth (백엔드가 토큰 교환·세션 발급).
- **연동**: GitHub API(미션 PR/커밋 조회, 단계적) · **기존 `greedy-discord-bot`(아래 8.1)** · Discord 웹훅(알림).
- **스토리지**: 미디어(추억/자료)는 오브젝트 스토리지(S3 호환) + DB엔 메타데이터.
- **배포**: 프론트 Vercel, 백엔드 운영진 택. CI에서 타입체크·빌드·OpenAPI 검증.

```
[Next.js]  ⇄  openapi.yaml (단일 계약)  ⇄  [백엔드(別언어)]
    │              │                            │
    │        타입 자동생성        GitHub API / greedy-discord-bot / Discord Webhook
    └─ 목 서버로 선행개발                        └─ DB · Object Storage
```

### 8.1 기존 디스코드 봇과의 통합 경계 (중요)
그리디는 이미 `greedy-team/greedy-discord-bot`(Java/JDA)을 운영 중. **재구현하지 않고 통합**한다.

봇이 이미 하는 것: **리뷰어 랜덤 매칭**(`/리뷰매칭`, `MatchingService`+`RandomShuffleStrategy`), **구글폼 응답 감시**(`GoogleFormWatch`), 예약 메시지, 정산, 운세, 스터디룸, 디스코드 역할 관리.

**현재 한계(=웹이 메울 부분)**
- 매칭 결과가 `toDiscordAnnouncement()` 문자열로 **디스코드 공지만** 되고 **영구 기록이 없다** → 완주율·발자취 추적 불가(P1의 근원).
- 리뷰이/리뷰어 명단이 봇 코드에 **하드코딩** → 매 라운드 손수정.

**통합 원칙: 봇=액션, 웹=기록.**
| 흐름 | 주체 | 내용 |
|---|---|---|
| 명단 제공 | 웹 → 봇 | 봇이 매칭 시 리뷰이/리뷰어 풀을 **웹 API에서 조회**(기수 멤버·`roleInCohort=REVIEWER`). 하드코딩 제거. |
| 매칭 기록 | 봇 → 웹 | `/리뷰매칭` 확정 시 결과를 **웹 API로 POST** → `ReviewAssignment`로 영구 저장. |
| 지원 수집 | 봇 → 웹 | 구글폼 응답 감지 시 **웹 API로 전달** → `Application` 생성(또는 웹 자체 폼으로 대체). |
| 알림 | 웹 → 봇/웹훅 | 제출·리뷰·머지 이벤트를 디스코드로 통지. |

> 효과: 봇의 검증된 기능은 그대로 두고, **휘발되던 결과를 웹이 역사로 남긴다.** 하드코딩 유지보수도 소멸.

---

## 9. MVP 범위 & 로드맵

원칙: **한 번에 다 만들지 않는다.** 가치가 가장 큰 미션 추적부터.

- **MVP (1차) — "로그인하면 미션이 추적된다"**
  - 인증(GitHub) + 역할(글로벌 3단계 + 기수 역할) + **권한 세분화 관리 UI** + 미션(정의·PR 링크 등록·리뷰어 배정·상태/마감일·현황 매트릭스) + 연혁 + 멤버(기수별 역할 발자취) + 공개 홈.
- **2차 — 스터디 & 게시판**
  - 스터디 자료/커리큘럼, 게시판(글·댓글·공지).
  - 미션 GitHub 연동 2단계(PR 상태·리뷰 코멘트 자동 동기화).
- **3차 — 지원 플로우**
  - 공개 지원 폼·심사 보드·멤버 전환.
- **4차 — 추억 저장소 & 미션 자동화**
  - 미디어 업로드·앨범, 리뷰 기간 종료 시 자동 머지 트리거, 요구사항 자동검증(3단계).

---

## 10. 비기능 요구사항
- 반응형 + **다크모드**, 웹 접근성 기본 준수.
- 권한 서버 강제(프론트는 편의 숨김만).
- OpenAPI 단일 계약 → 타입 자동생성·목 서버 병렬개발.
- 성능: 공개 페이지 SSR/캐싱으로 빠른 첫 화면.
- 관측: 에러 로깅·기본 분석.

## 11. 디자인 방향 (미정 — 동아리와 합의)
- 동아리 **공식 브랜딩**은 기존 개인 서비스 톤을 참고하지 않고, 그리디 정체성으로 별도 합의 후 확정.
- 본 PRD는 기능·구조에 집중하고, 비주얼/브랜딩은 별도 디자인 트랙에서 진행.

---

## 12. 리스크 & 가정
- **R1. FE/BE 사람이 다름** → 계약(OpenAPI) 미합의 시 재작업 위험. *완화*: 코드 전에 스펙·목 서버부터.
- **R2. 미션 PR 자동매칭 정확도** → username·제목 규칙 의존. *완화*: 1단계는 수동 등록 허용, 규칙 문서화.
- **R3. 운영 인력 지속성** → 유지보수 부담. *완화*: 관리 화면 단순화, MVP 범위 엄수.
- **R4. 개인정보(지원서)** → 수집·보관 동의 필요.
- **R5. 봇↔웹 계약** → 봇 명단을 웹에서 조회/결과 POST하도록 봇 수정 필요(봇 운영자와 합의). *완화*: 봇 변경 없이 "수동 기록"으로도 동작하게 설계, 통합은 단계적.
- **가정**: 회원 대부분 GitHub 계정 보유, Discord를 일상적으로 사용, 봇 운영자와 협업 가능.

---

## 13. 열린 질문 (티키타카 대상)
- ~~Q1. 미션 통과 판정~~ → **확정**: approve=통과가 아님. 리뷰어 랜덤 배정 → 티키타카 → 리뷰 기간(연장 가능) 종료 시 자동 머지=완료.
- ~~Q2. 기수 모델~~ → **확정**: 입회 기수(학기당 1, 연 2기, 현재 4기). 기수별 역할(메인테이너/리드/리뷰어/멤버) 발자취 표기.
- ~~Q6. 권한 세분화 UI~~ → **확정**: MVP 포함.
- ~~Q3. 지원 심사 단계~~ → **확정**: 서류 → 면접 → 합격 단계 심사(`SUBMITTED→DOC_PASS→INTERVIEW→ACCEPTED|REJECTED`).
- ~~Q7. 리뷰어 배정~~ → **확정**: 기존 디스코드 봇이 랜덤 매칭, 웹은 결과를 기록(통합, 8.1). 명단은 웹에서 조회.
- ~~Q8. 리뷰 기간~~ → **확정**: 동아리 공통 기본값(ClubConfig) + 미션별 예외 + 리뷰어 연장.
- Q4. 추억 저장소 미디어 용량/보관 정책(스토리지 비용). *(미해결)*
- Q5. 게시판 공개 범위(공지만 공개 vs 일부 공개). *(미해결)*
- ~~Q10. 지원 수집 방식~~ → **확정**: 웹 자체 폼.
- Q9. 봇 통합 범위/시점 — 봇을 바로 고칠지, 웹 수동기록으로 시작 후 단계적 통합할지. *(미해결, 봇 운영자와 협의)*

---

## 부록 A. 데이터 모델 (초안)
```
User(id, githubId, username, avatar, role, joinCohortId, bio, createdAt)   # role = GUEST|MEMBER|STAFF
UserPermission(userId, permissionKey)            # 운영진이 개별 부여한 권한
Cohort(id, name, season, year)                    # 기수 (예: name=4기, season=2026-1)
CohortMember(cohortId, userId, roleInCohort)      # roleInCohort = MEMBER|REVIEWER|LEAD|MAINTAINER
HistoryEvent(id, date, title, body, cohortId, mediaUrl[])
StudyItem(id, week, stage, title, body, fileUrl, authorId, tags[])
ClubConfig(key, value)                            # 전역 설정 (예: defaultReviewDays=7)
Mission(id, title, description, requirements[], reviewDaysOverride?, cohortId, repoUrl, stageLabel, createdBy)
Submission(id, missionId, userId, prUrl, status, reviewDueAt, extendedCount, rounds, mergedAt, submittedAt)
   # status = SUBMITTED | IN_REVIEW | MERGED   (approve가 아니라 기간 종료 머지 = 완료)
ReviewAssignment(id, submissionId, reviewerId, assignedAt, source)  # source = BOT | MANUAL (봇 매칭 결과 영구 기록)
Post(id, category, title, body, authorId, pinned, createdAt)
Comment(id, postId, authorId, body, createdAt)
Application(id, applicantInfo, stage, reviewerId, formResponseId?, createdAt)
   # stage = SUBMITTED | DOC_PASS | INTERVIEW | ACCEPTED | REJECTED
Album(id, title, cohortId, visibility)
Media(id, albumId, url, type, uploaderId, caption)
```

## 부록 B. 권한 매트릭스 (초안)
| 도메인 | GUEST | MEMBER | STAFF |
|---|---|---|---|
| 연혁 | 읽기 | 읽기 | 전체관리 `history:edit` |
| 멤버 | 공개프로필 | 본인수정 | 관리 `member:manage` |
| 스터디 | — | 읽기+업로드 | 커리큘럼 `study:manage` |
| 미션 | — | 제출·조회 | 출제 `mission:create` · 리뷰어배정 `mission:assign` · 리뷰 `mission:review`(배정 리뷰어) |
| 게시판 | 공개글 | 작성+댓글 | 모더레이션 `board:moderate` |
| 지원 | 제출 | 본인조회 | 심사 `recruit:review` |
| 추억 | 공개갤러리 | 업로드 | 큐레이션 `gallery:curate` |
| 역할/권한 | — | — | `admin:roles` |

## 부록 C. 미션 상태머신
```
[미션]   DRAFT ── 공개 ──> OPEN ── 마감 ──> CLOSED

[제출]   (없음)
           │ 멤버가 PR 제출
           ▼
        SUBMITTED ── 리뷰어 랜덤 배정 ──> IN_REVIEW
                                          │  멤버↔리뷰어 티키타카 (N라운드)
                                          │  reviewDueAt = 기본기간, 리뷰어가 연장 가능(extendedCount++)
                                          ▼  리뷰 기간 종료 → 자동 머지
                                        MERGED (완료)
```
- *approve는 종료 신호가 아니라 대화의 일부.* 완료 판정 = **리뷰 기간 종료에 따른 머지**.
- 알림 트리거: SUBMITTED → 배정된 리뷰어 / 새 리뷰 코멘트 → 상대방 / MERGED → 멤버.

---

## 부록 D. 변경 이력 (Changelog)
- **v0.1** — 초기 기획 baseline. 7개 도메인·역할 2축(글로벌+기수)·미션 리뷰 모델(랜덤배정→티키타카→기간종료 머지)·기존 디스코드 봇 통합 경계(8.1)·OpenAPI 계약·MVP 로드맵 확정. 지원 단계심사·웹 자체 폼·리뷰 기간 ClubConfig 반영.
