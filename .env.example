MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=greedy-hub
MOCK_SCREENSHOTS=true
# MICROLINK_API_KEY=  # 선택 — 없으면 무료 50건/일 적용
GITHUB_TOKEN=  # GitHub PAT (필수 권장) — 없으면 60req/hr 제한, 있으면 5000req/hr
