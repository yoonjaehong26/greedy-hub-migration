import { ActivityTimeline } from '@/features/activities/ActivityTimeline';

export const metadata = { title: '활동 타임라인 — 그리디 허브' };

export default function GalleryPage() {
  return <ActivityTimeline />;
}
